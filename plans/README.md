# Implementation Plans

Generated by the improve skill on 2026-08-24. Each executor: read the plan fully
before starting, honor its STOP conditions, and update your row when done.

All commands in the plans run from the `automation-platform/` directory.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001  | Delete the stale `commands/database.rs` orphan | P1 | S | — | DONE |
| 002  | Test the `useScan` scan-controller hook | P1 | M | — | DONE |
| 003  | Escape backticks in `generateMarkdown` inline code | P2 | S | — | DONE |
| 004  | Validate work-item priority and type at the persistence boundary | P2 | S | — | DONE |
| 005  | Give each selection a stable client id | P2 | S | — | DONE |
| 006  | Surface persistence failures and reconcile optimistic updates | P1 | M | 005 | DONE |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) | REJECTED (with one-line rationale)

## Dependency notes

- 001–003 are complete and merged into `main` (`8ac4723`, `c75f8ac`, `16e30cd`).
- 004, 005, 006 all edit `automation-platform/src/inspector/InspectorContext.tsx`,
  so execute them **sequentially, not in parallel**, to avoid merge churn.
  Recommended order: 005 (smallest, gives stable ids) → 004 → 006.
- 006 lists 005 as a soft dependency: stable ids make its optimistic-revert
  restore unambiguous, though 006 also works standalone.

## Findings considered and rejected

- **Path traversal via `folder_path` in `absolute_case_path`**: rejected — the
  value is always a single immediate-child name from `read_dir` (never a path
  separator), the workspace DB is the user's own local file, and the scanner is
  documented read-only. No in-scope vector.
- **`clipboard.ts` (`copyText`) untested**: rejected — thin wrapper over
  `navigator.clipboard` / `document.execCommand`; a test would exercise the
  browser API, not app logic. Low value.
- **Markdown "untested" claim (original finding #3)**: corrected during
  planning — `markdown.ts` and `identify.ts` *are* already tested in
  `inspector.test.ts`. The real gap was backtick escaping, which is plan 003.

## Formerly deferred backlog (now planned)

The three items the maintainer had queued are now plans 004–006:

- Priority model → 004 (validate priority/type on write + read)
- Selection ID cleanup → 005 (stable client id)
- Persistence error handling → 006 (surface + revert)
